# Core dependencies for AI Complaint Handler
streamlit==1.34.0
pandas==2.2.1
numpy==1.26.4
openpyxl==3.1.2
requests==2.31.0
Pillow==10.3.0

# Note: OCR libraries (paddlepaddle, paddleocr, opencv) removed due to Streamlit Cloud installation limits.
# Re-added via lightweight API or external service in future versions.
